version: 0.0
os: linux
files:
  - source: /index.html
    destination: /usr/share/nginx/html/
    overwrite: true
hooks:
  BeforeInstall:
     - location: Scripts/install
       timeout: 300
       runas: root
  ApplicationStop:
     - location: Scripts/stop
       timeout: 300
       runas: root
